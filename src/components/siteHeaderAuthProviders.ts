import type { SiteHeaderAuthProvider } from "./SiteHeaderAuthProvider.ts"

export const siteHeaderAuthProviders: readonly SiteHeaderAuthProvider[] = [
  { id: "google", label: "Weiter mit Google" },
  { id: "apple", label: "Weiter mit Apple" },
]
